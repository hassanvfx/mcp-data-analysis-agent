# Analysis recipes

Recipes are reviewed, Git-native TOML files. Each recipe declares an approved source alias,
parameterized read-only SQL, allowed parameters, and a chart preference. Execute recipes through
the same validation and ledger path as interactive queries.
